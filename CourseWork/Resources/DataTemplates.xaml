<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CourseWork.ViewModels" xmlns:local1="clr-namespace:CourseWork.Views">
    <DataTemplate DataType="{x:Type local:SearchViewModel}">
        <local1:SearchView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type local:HomeViewModel}">
        <local1:HomeView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type local:CartViewModel}">
        <local1:CartView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type local:AdminViewModel}">
        <local1:AdminView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type local:ProfileViewModel}">
        <local1:ProfileView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type local:SettingsViewModel}">
        <local1:SettingsView/>
    </DataTemplate>
</ResourceDictionary>