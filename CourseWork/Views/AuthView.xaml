<Window x:Class="CourseWork.Views.AuthView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CourseWork.Views"
        xmlns:viewmodel ="clr-namespace:CourseWork.ViewModels"
        mc:Ignorable="d"
        Title="AuthView" Height="450" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="142*"/>
            <RowDefinition Height="170*"/>
            <RowDefinition Height="41*"/>
            <RowDefinition Height="34*"/>
            <RowDefinition Height="33*"/>
        </Grid.RowDefinitions>
        <Grid Margin="0,0.6,-0.4,8.8" Grid.Row="1">
            <TextBox x:Name="login_textbox" Text="{Binding login}"/>
            <PasswordBox x:Name="password_textbox" Height="50" PasswordChanged="PasswordBox_PasswordChanged"/>
            <Button Height="40" Margin="0,105,0,16" Command="{Binding AuthCommand}"/>
        </Grid>
    </Grid>
</Window>
